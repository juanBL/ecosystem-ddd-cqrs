nelmio_api_doc:
  documentation:
    info:
      title: App Developer API
      description: App Developer API
      version: 1.0.0
    components:
      securitySchemes:
        Bearer:
          type: http
          scheme: bearer
          bearerFormat: JWT
    security:
      - Bearer: [ ]
  areas: # to filter documented areas
    path_patterns:
      - ^/api(?!/doc$)
      - ^/login
